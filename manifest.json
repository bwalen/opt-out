{
  "manifest_version": 2,

  "name": "Garbage Man",
  "description": "This extension searches the current page and deletes elements which contain words or phrases to be filtered out",
  "version": "1.0",
  "content_scripts": [
   {
     "matches": ["http://*/*", "https://*/*"],
     "js": ["underscore.js","underscore.string.min.js","default.js"]
   }
 ],
  "permissions": [
    "activeTab"
  ]
}
